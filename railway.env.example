# Railway / .env example for FareGlitch
# Fill these values in Railway environment variables (do NOT commit secrets)

# Core API / credentials
AMADEUS_API_KEY=
AMADEUS_API_SECRET=
AMADEUS_ENV=production  # or 'test'
API_SECRET_KEY=replace-with-secure-random-string

# Database (Railway Postgres plugin will provide DATABASE_URL)
DATABASE_URL=postgresql://user:password@host:5432/fareglitch
DATABASE_POOL_SIZE=10

# Redis (optional, for Celery/background jobs)
REDIS_URL=redis://:password@host:6379/0

# HubSpot
HUBSPOT_API_KEY=
HUBSPOT_PORTAL_ID=
HUBSPOT_TABLE_ID=

# SMS / Notifications
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
SINCH_SERVICE_PLAN_ID=
SINCH_API_TOKEN=
SINCH_PHONE_NUMBER=
YOUR_PHONE_NUMBER=
SLACK_WEBHOOK_URL=
ALERT_EMAIL_TO=

# SMTP (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Monitoring / Sentry
SENTRY_DSN=

# Feature flags (true/false)
ENABLE_AUTO_PUBLISH=false
ENABLE_SMS_ALERTS=true
ENABLE_INSTAGRAM_POSTING=true
ENABLE_SLACK_ALERTS=false
DEBUG_MODE=false

# Other (keep defaults in config.py unless overriding here)
API_HOST=0.0.0.0
API_PORT=8000

# Notes:
# - Add Railway Postgres plugin and Railway Redis plugin. Railway will set DATABASE_URL and REDIS_URL automatically.
# - Set `startCommand` to `python start.py` (railway.json already contains this).
# - The Dockerfile exposes port 8000 and defaults to using the PORT env var if provided by Railway.
# - Do NOT store real secrets in the repo; paste them into Railway's Environment settings.
